import { createContext } from "react";

/**
 * @context Initializes a user context to be accessible through the entire project
 * @returns {React.Context}
 */
export const UserLoggedInContext = createContext(null);
